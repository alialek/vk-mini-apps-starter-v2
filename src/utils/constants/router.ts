export const DEFAULT_ROOT = 'default_root';
export const DEFAULT_VIEW = 'default_view';

export const PANEL = {
    DAILY_WISH: 'daily_wish_panel',
    DIARY: 'diary_panel',
    LIBRARY: 'library_panel',
} as const;

export const PATH = {
    DAILY_WISH: '/',
    DIARY: '/diary',
    LIBRARY: '/library',
} as const;
